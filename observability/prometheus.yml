global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'notification-pipeline'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
        # host.docker.internal lets the container reach your localhost
        # where Spring Boot is running